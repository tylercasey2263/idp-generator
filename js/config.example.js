// ─── SUPABASE CONFIG TEMPLATE ─────────────────────────────────────────────
// Copy this file to js/config.js and fill in your real values.
// js/config.js is gitignored — never commit real credentials.
//
// Find these values in: Supabase Dashboard → Settings → API
//   url     → Project URL
//   anonKey → anon / public key  (safe for browser use — not the service role key)

const SUPABASE_CONFIG = {
  url:     'https://YOUR_PROJECT_ID.supabase.co',
  anonKey: 'YOUR_SUPABASE_ANON_KEY',
};
